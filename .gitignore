node_modules
.next
.DS_Store
.vercel
*.tsbuildinfo
next-env.d.ts

# Database credentials. .env.local holds the Neon connection string; nothing
# in this family should ever be committed.
.env
.env.*
!.env.example

# Open Hymnal source files, re-fetchable with scripts/fetch-open-hymnal.sh.
# ~2.7MB of upstream data; the built other-songs.json is what ships.
vendor/
